[pytest]
# pytest-django 需要知道用哪个 Django settings。
DJANGO_SETTINGS_MODULE = careplan_project.settings
# 只把 test_*.py 当测试文件 (避免误抓 manage.py 等)。
python_files = test_*.py
# -v: 列出每个测试名; --reuse-db: 复用测试库, 第二次跑不重建 schema, 快很多。
addopts = -v --reuse-db
